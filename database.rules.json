{
  "rules": {
    ".read": false,
    ".write": false,
    "sessions": {
      ".read": "auth != null && auth.uid === 'REPLACE_WITH_YOUR_UID'",
      ".write": "auth != null && auth.uid === 'REPLACE_WITH_YOUR_UID'"
    }
  }
}
